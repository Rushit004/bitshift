#ifndef CONVERTER_H
#define CONVERTER_H

double toDecimal(char number[], int base);
void fromDecimal(double decimal, int base, char result[]);

void ConverterModule();  // already used in main

#endif